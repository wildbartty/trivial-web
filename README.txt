This will be a native network interfacing library for common lisp.
It will support http, https, ftp, and rsync to begin with.
In its current state it only supports sbcl, but will support other implementations
